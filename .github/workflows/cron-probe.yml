name: cron-probe
on:
  workflow_dispatch:
  schedule:
    - cron: "*/5 * * * *"
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo "event=${{ github.event_name }} at $(date -u +%FT%TZ)"
